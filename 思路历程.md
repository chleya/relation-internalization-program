从关系内化到前语言操作结构：连续世界中可行动结构的形成、诊断与生成问题

摘要

本文整理并形式化了一条从“关系内化”到“前语言操作结构内化”的研究路线。最初的问题是：一个模型是否真的“内化”了关系，还是仅仅在输入输出层面表现出关系语言或关系行为？随着讨论推进，我们发现“关系”不能只被理解为语言层面的符号关系表，因为现实中的事实总是先于语言发生：小球先运动、遮挡、碰撞、反弹，然后人类才用“对象”“事件”“因果”“关系”等语言去覆盖它。因此，真正底层的问题不是语言系统是否会说关系，而是系统能否从连续事实流中形成可行动的内部操作结构。

我们将原有 relation internalization 框架修正为 W → O₁ → O₂ → L：其中 W 是连续事实世界，O₁ 是轨迹图式、对象持续性、delayed trace、checkpoint 等前语言操作痕迹，O₂ 是 inspect target、intervention target、action type、closed-loop update、actionability mask 等可行动操作结构，L 则是语言覆盖与审计报告。基于这一框架，我们构建了 B 线 PLOS-Test，即 Pre-Linguistic Operational Structure Test，从短程 checkpoint 开始，逐步推进到 delayed trace、private selector、active inspection、intervention、action-type disambiguation、clean closed loop、content-sensitive update / feedback revision，以及 actionability-aware risk-constrained operation。每一个正结果之后，我们都加入 degeneracy audit，审计其是否来自 shared selector、same-plan degeneracy、fixed action shortcut、oracle leakage、scripted update、weak baseline 或显式标签捷径。

本文的核心结论是：当前 B 线并不是死胡同，而是一条有效的“判别器路线”：它已经形成了一套强审计的前语言操作结构诊断链。但它还没有回答最初更深的问题，即操作结构如何自然生成。B 线证明的是“如果我们构造或提供某些结构通道，系统能否在 toy diagnostics 下使用并通过审计”；它尚未证明“系统能否在预测、压缩、检查成本、干预成本、反馈修正和风险约束等压力下，自发析出这些结构”。因此，下一阶段应从 B-line discriminator route 转向 G-line generator route：设计一个最小生成器实验，在不提供手写 trace selector、explicit actionability mask、explicit relation table 或 oracle value 的前提下，让系统仅从连续世界、压缩压力、预测压力、inspect cost、intervention cost、feedback loss 和 experienced risk/cost consequences 中自行形成可复用的操作结构。

⸻

1. 引言：从“模型懂关系吗”到“结构如何发生”

一开始的问题很朴素：

现在的模型，尤其是 LLM，到底有没有内化关系？

这看似是一个关于大模型理解能力的问题，但很快变成了一个更深的问题：什么才配叫“内化”？

如果模型只是看到 A → B 的样本后，在相似输入下输出 B，这可以叫反应，不能叫强内化。如果模型能在新符号、新 context、新 support 下仍然使用同一关系，已经比单纯记忆强。但这仍不够。我们进一步要求：

1. symbol transfer：换符号仍能迁移；
2. support-conditioned binding：同一 query 在不同 support 下输出应改变；
3. local edit：局部关系修改只影响相关输出；
4. exact audit：能指出哪条关系链不确定；
5. budgeted inspect：观测预算有限时知道该查哪个变量。

这就是最初的 A 线：symbolic relation internalization。它的核心是判断系统内部是否存在某种类似 R_hat 的关系结构，而不是单纯记住输入输出模式。

但后来我们意识到，这个框架仍然太语言化、太表格化。它默认关系已经是某种可命名、可列举、可编辑的对象。可是现实不是这样开始的。

现实是：

小球先滚动；
小球先被遮挡；
小球先碰撞；
小球先改变轨迹；
边坡先渗水；
孔压先升高；
裂缝先扩张；
位移先累积；
然后人类才用语言说：
“对象持续性”
“碰撞事件”
“因果关系”
“风险点”
“干预措施”

所以真正底层的问题不是：

模型是否掌握了语言关系？

而是：

系统能否在语言之前，从连续事实流中形成可行动的操作结构？

这就是 B 线的起点。

⸻

2. 核心修正：事实先发生，语言后覆盖

我们最终形成了一个关键修正：

事实先发生，语言符号后覆盖。

语言不是结构的本体。语言是结构的外显、压缩、沟通和审计方式。

这意味着，不能因为一个系统会说：

“小球被遮挡后仍然存在。”
“碰撞改变了运动方向。”
“这个变量影响那个变量。”
“我应该检查这个区域。”

就判定它拥有操作结构。

真正要看的是：

它是否在没有语言标签的情况下保持对象身份？
它是否在遮挡后维持轨迹？
它是否能识别事件边界？
它是否知道哪里值得查？
它是否知道哪里可以动？
它是否知道动错了有什么代价？
它是否能根据检查和反馈更新内部状态？

由此，原来的关系内化框架被修正为：

W → O₁ → O₂ → L

其中：

W：continuous world / fact occurrence layer
O₁：trajectory schema / delayed trace / checkpoint
O₂：actionable operational structure
L：language cover / audit layer

⸻

3. W → O₁ → O₂ → L 框架

3.1 W：连续事实层

W 是世界本身的连续过程。

它不是智能，不是模型，不是语言。它是事实流：

小球滚动；
遮挡出现；
碰撞发生；
力场改变轨迹；
延迟效应在未来显现；
水进入裂隙；
孔压升高；
坡体缓慢位移；
裂缝扩张。

这一层没有“对象”“事件”“关系”这些概念。它只是发生。

3.2 O₁：轨迹图式与 delayed trace

O₁ 是系统从连续事实流中压缩出的前语言操作痕迹。

它包括：

对象持续性；
运动连续性；
遮挡后延续；
交叉后身份保持；
碰撞边界；
delayed trace；
checkpoint；
短期和延迟影响。

这一层已经超出单纯像素预测。像素预测模型可能会预测下一帧，但并不一定有对象身份、事件边界、延迟痕迹或可行动 checkpoint。

O₁ 回答的问题是：

什么东西持续存在？
什么东西改变了？
哪个影响被延迟了？
哪个地方是未来变化的隐藏原因？
哪里值得记住？

3.3 O₂：可行动操作结构

O₂ 是 O₁ 被转化为行动结构后的层级。

它包括：

inspect target；
intervention target；
action region；
action type；
wrong-region penalty；
wrong-action penalty；
risk / cost；
abstain；
direct / indirect intervention；
feedback revision。

O₂ 回答的问题是：

我该查哪里？
我该不该查？
我该动哪里？
我该怎么动？
这个地方能不能动？
动了是否危险？
动作是否不可逆？
如果不能直接动，能否间接动？
如果风险太高，是否应该 abstain？

3.4 L：语言覆盖与审计层

L 是语言描述层。

它可以写成：

“该区域是 delayed trace region。”
“这个动作风险过高，应放弃。”
“检查结果改变了后续干预计划。”
“反馈结果表明原先 trace 估计错误。”

语言层有价值，因为它让人类能审计、复盘和交流。但它不是结构本体。

关键区别是：

L → L：语言反应；
W → O₁ → O₂ → L：事实流中形成操作结构，再被语言覆盖。

LLM 主要强在 L → L。它可能能模拟 O 的语言描述，但是否拥有从 W 中形成 O 的机制，目前没有证明。

⸻

4. 重新定位 LLM

在这个框架下，LLM 的位置变得清楚：

LLM prompt-only：
强语言反应器；
强统计压缩器；
强模式组合器；
但不是已证明的前语言操作结构形成器。

LLM 肯定不是“空表”。它有大量弱内化：

语义模式；
常识片段；
统计关系；
语言中的因果模板；
世界描述的压缩。

但我们这里的标准更强。我们问的是：

它能否在随机符号下稳定 transfer？
能否 support-conditioned binding？
能否 local edit 后局部行为改变？
能否 exact audit？
能否 budgeted inspect？
能否从连续事实流中形成对象、事件、trace、checkpoint？
能否不靠语言标签进行 inspect / intervene / feedback revision？

因此，LLM 的当前定位是：

强一阶 + 强语言层；
可能有弱统计内化；
但强结构内化未证明；
前语言操作结构内化更未证明。

这并不是说 LLM 是“石头”。更准确地说，它可能是“半鸡蛋”：有某些承载结构的潜能，但仅靠 prompt 不能证明它已经孵出了我们要的操作结构。

⸻

5. 鸡蛋型基底与石头型基底

我们曾用一个比喻修正“训练能产生内化”的说法：

鸡蛋能孵出小鸡，是因为它本来就是鸡蛋；石头不能；煮砂不能成饭。

这意味着：

外部训练压力不是魔法；
数据不是魔法；
prompt 不是魔法；
环境不是魔法。

一个系统能否内化结构，取决于：

1. 基底是否能承载结构；
2. 学习机制是否能改变内部组织；
3. 外部压力是否能区分结构与假象；
4. 反馈是否能使结构被修正和稳定。

因此，内化不是：

任意系统 + 足够训练 = 结构内化

而是：

可内化基底 + 结构化世界 + 合适压力 + 可验证反馈
→ 可能形成操作结构

这后来成为从 B 线转向 G 线的理论基础。

⸻

6. 一阶、二阶、三阶系统

我们把系统分成不同阶层。

6.1 零阶：事实发生

世界自己演化：

小球动；
水流动；
裂缝扩展；
边坡位移。

没有系统，没有理解。

6.2 一阶：反应 / 预测

系统能把输入映射到输出：

input history → prediction / reaction

典型系统：

MLP；
查表；
if-else；
像素预测模型；
轨迹记忆模型；
普通 RNN / Transformer world model；
LLM prompt response。

一阶可以很强，但问题是：

没有可分离操作结构；
对象交叉后可能换身份；
遮挡后可能丢对象；
不知道该查哪里；
不知道干预哪里；
不能局部编辑；
不能解释不确定性所在。

6.3 二阶：操作结构内化

二阶系统形成内部中介 O：

W → O → prediction / inspect / intervention / action

它不仅输出答案，还能用内部结构支撑多个操作：

预测；
检查；
干预；
反馈修正；
局部编辑；
风险规避。

6.4 三阶：元操作结构修正

三阶系统不仅能改 O，还能改“形成 O 的方式”。

例如：

我总是用颜色追踪对象，应该改用运动连续性；
我总是追显著区域，应该追信息价值最高区域；
我总是把接近误判为碰撞；
我总是忽视延迟反馈。

当前项目大部分仍在二阶诊断范围。三阶尚未真正开始。

⸻

7. A 线与 B 线

7.1 A 线：符号关系内化

A 线关注：

relation table；
support-conditioned binding；
local edit；
exact audit；
budgeted variable inspect。

它适合工程审计、AI agent、符号关系系统。

它的核心问题是：

系统是否持有可迁移、可编辑、可审计、可行动的关系结构？

7.2 B 线：前语言操作结构内化

B 线关注：

object persistence；
event boundary；
delayed trace；
checkpoint；
inspect target；
intervention target；
action type；
closed-loop update；
risk-constrained actionability。

它适合连续世界、机器人前置能力、物理场景、工地/边坡类问题。

它的核心问题是：

系统是否能从连续事实流中形成前语言操作结构？

⸻

8. B 线证据阶梯：从 PLOS 到 B6

B 线形成了一个逐层证据阶梯。

8.1 PLOS v1：短程 checkpoint candidate

最初的 PLOS-Test 测试前语言操作 checkpoint。flow_checkpoint_model 成为初始候选。

它说明：

短程 flow/checkpoint 机制能捕捉一些局部操作线索。

但它不能说明 delayed trace、active inspection 或 intervention。

8.2 B1.1：短程 checkpoint 被 delayed checkpoint 打穿

B1.1 加入：

dynamic decoy checkpoint；
delayed checkpoint；
competing checkpoints；
checkpoint relocation OOD；
causal deletion vs visual deletion；
anti-prior world。

结果显示 flow_checkpoint_model 处理不了 delayed checkpoint。

结论：

短程 checkpoint substrate 不够；
需要 trace-bearing substrate。

8.3 B2：Delayed Operational Checkpoint Substrate

新增：

recurrent_flow_checkpoint_model；
field_memory_model；
schema_memory_model。

解决 delayed checkpoint 问题。

但一开始不能证明三种机制独立。

8.4 B2.1 / B2.1a / B2.2 / B2.3

B2.1 高分后，B2.1a 发现三模型同预测。B2.2 进一步指出 shared selector 风险。B2.3 重构 private selector，使：

shared_selector_usage_rate = 0；
cross_model_exact_prediction_match_rate = 0；
disagreement_episode_divergence = 1。

结论：

trace-bearing path 有用；
shared selector 解释被削弱；
出现 toy diagnostics 下的部分机制分离。

8.5 B3：Delayed Trace-Guided Active Inspection

B3 测试 delayed trace 是否能指导 inspect。

结果高分，但 B3.1 发现三模型同选 inspect region。B3.2 加入 family-specific inspection targets，解决 same-region degeneracy。

结论：

delayed trace 可以指导 active inspection；
机制区分的 active inspection 在 toy diagnostics 下成立。

8.6 B4：Intervention 与 Action Type

B4 测试 trace 是否能指导 intervention。B4.1 发现 fixed action-type shortcut。B4.2 加入 action-type-specific targets 后通过：

fixed_action_type_rate 从 1.000 降到 0.500；
action_type_accuracy = 1.000；
correct_region_wrong_action_penalty = 0.900；
action_type_counterfactual_sensitivity = 0.900。

结论：

trace 不仅能指导动哪里，也能在 toy diagnostics 下指导怎么动。

8.7 B5：Closed Loop 与泄漏问题

B5 构造：

observe → inspect → update trace → intervene → observe consequence → revise trace

初始高分，但 B5.1 发现：

value_leakage_count = 800；
same-plan degeneracy；
scripted update；
scripted feedback。

这说明原 B5 不能作为真实闭环证据。

8.8 B5-Clean

B5-Clean 修复：

model_input；
evaluator_ground_truth；
oracle_baseline_view。

实现三层隔离：

policy 只能看 sanitized model_input；
metrics 才能看 evaluator_ground_truth；
oracle baseline 才能看 oracle_baseline_view。

结果：

best_b5_clean_closed_loop_score = 0.925；
leakage = 0。

结论：

B5 恢复为 oracle-free closed-loop path diagnostic。

但 clean B5.1 仍发现 same-plan 与 scripted update / feedback 问题。

8.9 B5.2：Adaptive Trace Update and Feedback Revision

B5.2 专门打：

inspection-content swap；
counterfactual inspection observation；
same-initial-different-info plan divergence；
contradictory feedback；
delayed feedback；
update-vs-scripted baseline；
feedback-vs-scripted baseline；
revision-specific ablation。

结果：

best_b52_adaptive_update_score = 0.996；
value_leakage_count = 0；
inspection_content_sensitivity = 1.000；
counterfactual_update_switch_rate = 1.000；
same_initial_different_info_plan_divergence = 1.000；
model_gain_over_scripted_update ≈ 0.98；
feedback_content_sensitivity = 1.000；
model_gain_over_scripted_feedback ≈ 0.98；
cross_model_exact_plan_match_rate = 0。

结论：

在 clean/oracle-free toy diagnostics 下，
trace update 和 feedback revision 对 inspection/consequence content 敏感；
信息不同会导致 post-update plan 分流。

8.10 B6：Actionability Mask / Risk-Constrained Closed Loop

B6 加入 actionability mask：

observable；
inspectable；
directly_intervenable；
indirectly_intervenable；
unsafe；
irreversible；
costly。

系统需要在风险、成本、不安全、不可逆约束下选择：

inspect；
direct intervention；
indirect intervention；
abstain。

结果：

best_b6_risk_constrained_score = 0.986；
value_leakage_count = 0；
actionability_mask_accuracy = 1.000；
unsafe_action_rejection_rate = 1.000；
irreversible_action_rejection_rate = 1.000；
indirect_intervention_accuracy = 1.000；
abstain_when_required_accuracy = 1.000；
gain_over_risk_blind = 0.857。

结论：

在 toy PLOS 环境中，
clean B5.2 closed-loop 可以使用 actionability mask，
在 risk/cost/unsafe/irreversible 约束下选择 inspect、direct intervention、indirect intervention 或 abstain。

⸻

9. 反方审计：为什么每次高分后都要打掉它

项目最有价值的方法论不是“高分”，而是：

高分之后必须审计。

每一层都问：

是不是 shared selector？
是不是 same-plan degeneracy？
是不是 fixed action shortcut？
是不是 value leakage？
是不是 oracle field 泄漏？
是不是 scripted update？
是不是 feedback script？
是不是 weak baseline？
是不是 explicit label following？
是不是 ablation 非特异？

这使项目不像普通 benchmark 那样停留在 score，而是形成了一条 false-positive ladder。

典型例子：

B2.1 高分 → B2.1a 发现同预测；
B3 高分 → B3.1 发现同 inspect region；
B4 高分 → B4.1 发现 fixed action type；
B5 高分 → B5.1 发现 leakage；
B5-Clean 修复 leakage → clean B5.1 仍发现 scripted update；
B5.2 才解决 content-sensitive update / feedback。

这说明项目不是为了证明预设结论，而是在不断逼近：

什么才算真正的操作结构证据？

⸻

10. 当前最强 claim

目前允许的最强结论是：

在 toy PLOS 环境中，private delayed operational trace 可以支持 delayed checkpoint、private selector、active inspection、mechanism-disambiguated inspection、intervention-region selection、differentiated action-type selection、oracle-free closed-loop operation、content-sensitive trace update / feedback revision，并进一步在 actionability mask 下进行 risk-constrained inspect / intervene / abstain 决策。

更短：

B 线已经构造出一条从连续事实流到风险约束闭环操作的可审计 toy diagnostic chain。

⸻

11. 当前不能 claim 的东西

即使 B6 高分，也不能 claim：

真实控制能力；
机器人智能；
工程部署能力；
安全认证；
真实边坡/工地干预能力；
人类式 active inference；
general physical reasoning；
前语言智能已经解决；
自然涌现；
blank-slate 结构发生；
结构是系统自己长出来的。

这点必须压住。因为当前 B 线仍然是诊断系统，不是生成机制。

⸻

12. 我们是不是走到死胡同？

答案是：

不是死胡同，但判别器路线接近平台期。

为什么不是死胡同？

因为它产生了大量有用失败，并且每次失败都定位了假阳性来源。

为什么接近平台期？

因为越往后，结构越多是我们设计进去的：

trace family；
private selector；
inspection value；
intervention target；
action type；
trace update hook；
feedback revision hook；
actionability mask；
risk/cost fields。

系统在学习/使用这些结构，但这些结构并不是自然长出来的。

所以，B 线回答的是：

如果我们定义并提供操作结构通道，系统能否使用并通过审计？

但最初的问题是：

操作结构如何从连续世界中自己发生？

这就是 B 线与 G 线的分界。

⸻

13. 判别器路线与生成器路线

13.1 判别器路线

B 线是判别器路线：

定义结构；
设计 gate；
设计 audit；
排除假阳性；
形成 benchmark。

它的价值是：

让“模型是否有操作结构”变得可测；
让各种假象暴露；
为未来模型提供诊断工具。

13.2 生成器路线

下一阶段 G 线应问：

不给 trace selector；
不给 actionability mask；
不给 explicit relation table；
不给 oracle value；
只给连续世界 + 压缩压力 + 预测压力 + inspect cost + intervention cost + feedback loss；
系统能不能自己析出：
  delayed trace；
  checkpoint；
  inspect target；
  intervention target；
  actionability-like representation？

这才回到最终目标。

⸻

14. 最终目标

我们的最终目标应写成：

证明或证伪一个最小命题：在连续世界中，一个具有合适基底的系统，是否能在预测、压缩、检查、干预、反馈和风险压力下，自发形成可迁移、可更新、可行动的操作结构，而不是只靠语言、记忆或手写规则。

用公式化语言说：

W = continuous world stream
System receives:
  observations
  actions
  feedback
  costs
  risks
  limited memory
System must form:
  O = operational structure
O must support:
  prediction
  inspect
  intervention
  feedback revision
  risk-aware abstain
Without being given:
  trace labels
  relation table
  actionability mask
  oracle plan
  best action
  best region

⸻

15. G1：最小生成器实验的方向

G1 应是：

Minimal Generator of Operational Structure

核心原则：

不提供手写 trace selector；
不提供 explicit actionability mask；
不提供 relation table；
不提供 oracle value；
不提供 best_* 字段；
不提供 inspect target label；
不提供 intervention target label。

只提供：

连续 64x64 toy world；
预测压力；
压缩压力；
inspect cost；
intervention cost；
feedback loss；
experienced risk/cost consequences。

系统需要自己产生内部变量，这些变量在事后表现得像：

delayed trace；
checkpoint；
inspect target；
intervention target；
actionability-like constraint。

G1 的关键不是一开始就高分，而是：

是否出现可解码的 trace；
是否压缩后仍保留；
是否能指导 inspect；
是否能指导 intervention；
是否被反馈修正；
是否在 OOD 中保持；
ablation 是否特异。

⸻

16. 候选基底

G1 可考虑几类 substrate：

1. recurrent memory substrate
   让系统靠隐藏状态承载 trace。
2. field memory substrate
   让系统形成连续场式 memory。
3. slot / object-centric substrate
   让系统用竞争 slot 绑定对象/trace。
4. schema compression substrate
   让系统在压缩瓶颈下形成少数可复用图式。
5. hybrid substrate
   感知层连续，操作层离散，记忆层可编辑。

但 G1 不能把结构直接写进去。它只能提供压力，让结构有机会出现。

⸻

17. G1 的失败标准

G1 如果失败，也有价值。

失败可能说明：

1. 仅靠预测压力不够；
2. 压缩压力太弱；
3. inspect cost 没有逼出检查目标；
4. intervention loss 没有逼出行动结构；
5. feedback 太晚，无法 credit assignment；
6. 基底不是鸡蛋；
7. 我们仍需要更强结构先验。

这会直接回答“鸡蛋型基底”的问题。

⸻

18. 论文级贡献定位

这条研究路线的论文贡献不是：

我们做了一个更强模型。

而是：

我们把“内化”从语言关系问题推进到前语言操作结构问题；
提出 W→O₁→O₂→L 框架；
构建一条从 delayed trace 到 risk-constrained closed loop 的诊断阶梯；
通过多轮 degeneracy audit 排除常见假阳性；
指出当前判别器路线的边界；
提出下一阶段生成器路线。

这是一种“结构发生学”的研究纲领：

operational structure genesis

中文可以叫：

操作结构发生学

⸻

19. 结论

我们一路从“LLM 是否内化关系”出发，最终到达了一个更深的问题：

智能不是先会说关系，而是先能从事实流中形成可行动的操作结构。

B 线已经证明，我们可以把这种操作结构拆成一系列可审计的能力：

delayed trace；
private selector；
active inspection；
intervention；
action type；
closed-loop update；
feedback revision；
risk-constrained actionability。

但 B 线仍主要是判别器路线。它证明的是：

结构一旦被构造或提供，系统能否使用它并通过审计。

下一阶段必须转向生成器路线。真正的问题是：

结构能否在压力下自己长出来？

因此，项目没有走到死胡同。它走到了一个分水岭：

继续堆 B 线 → 更强 benchmark，但边际收益下降；
转向 G 线 → 回到最初问题，风险更高，但价值更大。

最终目标不是让系统通过更多 toy gates，而是让系统在连续世界中，在有限记忆、有限观察、有限行动、反馈延迟和风险成本下，自己形成可迁移、可更新、可行动的内部操作结构。

---

20. Causal Graph World 与涌现的上下界（2026-05）

经过 35 轮实验（物理世界 21 + 因果图世界 8 + 架构改造 6），我们找到了涌现的精确边界：

**架构级涌现**：可达。GNN 在 causal graph world 中 benefit=0.041（57% improvement），
OOD 泛化 116%，证明模型学到了"message passing 在热传导中普遍有用"这个架构知识。

**内容级涌现**：不可达。以下所有方法均失败：
- 边权重标量学习 → 全部坍缩到 0.5（均匀）
- L1 正则 → 推回 0.5 均衡点
- Top-K 竞争 → AUC=0.46，低于随机 0.5
- 非对称世界 + 多类型对象 → AUC=0.30
- Pairwise random coefficients → 权重仍坍缩到 0.5
- 纯粹边嵌入（移除 w_msg）→ AUC=0.0，corr=-0.10
- 消融重要性（离散法）→ Spearman r=0.048
- EdgeSpecificGNN（每条边独立 w_msg[j,i]）→ r=0.15（劣于基线）
- HyperEdgeGNN（超网络生成边权重）→ r=0.003~0.05
- AttnRouterGNN（多头注意力 + 边路由门控）→ 所有 route 坍缩为均匀分布
  h=4: route=[0.25,0.25,0.25,0.25]；h=8: route=[0.125,...]

**坍缩到均匀是数学必然，不是调参问题：**
- 信息论：边区分需要 O(log N_edges) 比特信号，单样本 loss 梯度只含 O(1/N) 比特
- 优化论：均匀解是梯度流的不动点，任何偏离增加方差 → 梯度推回均匀
- 架构瓶颈：共享 w_msg 对所有源对象做相同处理 → 温度差在任意边上等价
- 这是涌现的精确上下界：架构级可涌现，内容级不可涌现——在无导师条件下

**启示：**
- 梯度训练的预测误差最小化不是 O₂ 涌现的正确信号
- 真实大脑可能用惊奇/新颖性或行动后果而非预测误差来区分关系
- O(1/N) 梯度盲是深度学习中一个未被充分讨论的基本限制

---

21. 下一步探索方向（2026-05）

基于边界发现，四条路径并行探索：

A. 主动干预（Active Intervention）：把被动预测改为主动推→观察连锁反应。
   主动学习者天然需要知道边结构，可能绕过 O(1/N) 盲。

B. LLM 诊断：用 causal graph world 的自然语言版测 LLM 是否有同样边界。
   LLM 也是梯度训练的，如果它也跨不过内容级 → 发现影响面扩大。

C. 非梯度离散搜索：绕开梯度，用随机删边→测 loss→保留有用的结构。
   如果有效 → 证明内容级涌现需要非梯度机制。

D. 理论框架：架构级 vs 内容级涌现的区分本身就是新概念贡献。

这才是“内化”问题真正的底层形态。