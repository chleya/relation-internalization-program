# Notes

Start only after `relation-internalization-test` is frozen.

Primary risk:

```text
Probe readability may be mistaken for causal use.
```

Use intervention controls from `probe_to_boundary_gnn`.
